@page
@model WebAppFAM.Pages.Statuses.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/_LayoutDataTables.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Status[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status[0].CreatedUtc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Status) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedUtc)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.StatusID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.StatusID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.StatusID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
